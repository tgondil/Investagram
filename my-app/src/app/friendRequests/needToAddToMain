// add to my app.js or entry point file

const express = require('express');
const bodyParser = require('body-parser');
const friendRequestRoutes = require('./friendRequestRoutes'); // Update the path

const app = express();

app.use(bodyParser.json());

// Your existing routes...
// ...

// Use friend request routes
app.use('/api/friend-requests', friendRequestRoutes);

// Start your server...
